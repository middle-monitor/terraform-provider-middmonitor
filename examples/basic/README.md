# `basic` example

1. Copy `main.tf` and replace `base_url`, `org_slug`.
2. Export the token: `export TF_VAR_middmonitor_access_token="eyJ..."`.
3. Configure the provider for local dev: see the *Build and install locally* section of the provider README.
4. `terraform init && terraform plan`
